# from django.db import models

# # Create your models here.
# from map.models import *


# class Node_MQTT(models.Model):
#     node = models.ForeignKey(Node, on_delete=models.CASCADE, null=True, related_name='nodes')


# class Data_MQTT(models.Model):
#     data = models.ForeignKey(Data, on_delete=models.CASCADE, null=True, related_name='datas')